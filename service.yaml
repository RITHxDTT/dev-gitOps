apiVersion: v1
kind: Service
metadata:
  name: dev-homework-service
  namespace: dev-homework

spec:
  type: NodePort

  selector:
    app: dev-homework

  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30080